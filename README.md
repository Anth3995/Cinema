# How it runs?

### into console run:

    docker compose up

### Swagger url:

    http://localhost:9999/documentation

### Run tests:

*linux/macos*:

    chmod +x gradlew && ./gradlew test

*windows:*

    gradlew.bat test
